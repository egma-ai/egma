egma-planning/
.claude/settings.local.json
node_modules/

dist/
.next/
# Who is writing .next right now. See apps/web/tools/output-lock.ts.
.next.lock
*.tsbuildinfo
coverage/

.env
.env.*
!.env.example

# A local override of the compose file, copied from the checked-in example.
docker-compose.override.yml

# What `egma self-host up` and `egma self-host setup` leave in a platform
# workspace: the platform's own local configuration and the receipts beside
# it. A receipt carries no secret by construction — it is swept before it is
# written — but it is this deployment's history and not the product's.
.egma-platform/

.DS_Store

# Agent worktrees — full checkouts, never part of the repo
.claude/worktrees/
