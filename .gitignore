egma-planning/
.claude/settings.local.json
node_modules/

dist/
.next/
# Who is writing .next right now. See apps/web/tools/output-lock.ts.
.next.lock
*.tsbuildinfo
coverage/

.env
.env.*
!.env.example

# A local override of the compose file, copied from the checked-in example.
docker-compose.override.yml

# Private internal credentials generated by `egma self-host up` for this
# workspace. They belong to this deployment and must not enter the repository.
.egma-platform/

.DS_Store

# Agent worktrees — full checkouts, never part of the repo
.claude/worktrees/

# Screenshots and throwaway scripts an agent leaves behind while proving a
# visual change. They belong to the review, not to the repository.
.proofs/
