import { AwaitingArea } from "../awaiting.tsx";

export default function PersonasPage() {
  return (
    <AwaitingArea
      area="Persona authoring"
      title="Personas"
      what="The synthetic person who calls the agent: manner, patience, accent, speech rate, and behavior under friction."
      meanwhile="Every project has a default persona, and a test that names no persona uses it. Authoring personas arrives with Personas."
    />
  );
}
