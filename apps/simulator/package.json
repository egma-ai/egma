{
  "name": "@egma/simulator",
  "version": "0.0.0",
  "private": true,
  "description": "pnpm's door into the Python app: the real toolchain is uv. See README.md.",
  "scripts": {
    "lint": "uv run --frozen ruff check src tests",
    "test": "uv run --frozen ruff check src tests && uv run --frozen pytest",
    "gateway-latency": "uv run --frozen python tools/gateway_latency.py"
  }
}
