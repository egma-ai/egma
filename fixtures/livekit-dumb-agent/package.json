{
  "name": "@egma/livekit-dumb-agent",
  "version": "0.0.0",
  "private": true,
  "description": "pnpm's door into the fixture agent: the real toolchain is uv. See README.md.",
  "scripts": {
    "lint": "uv run --frozen ruff check .",
    "test": "uv run --frozen ruff check . && uv run --frozen pytest",
    "calendar-is-full": "./calendar-is-full.sh"
  }
}
